let $html = $('html');
let $body = $(document.body);
let $htmlBody = $('html, body');
let $window = $(window);

$(function () {
  $html = $('html');
  $body = $(document.body);
  $htmlBody = $('html, body');
  $window = $(window);
});

export { $html, $body, $htmlBody, $window };
